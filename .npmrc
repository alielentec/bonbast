# react-flagpack@2.0.6 declares React <=18 in peer deps, but works fine
# with React 19 (it's just an SVG wrapper). Allow npm to install with this
# mismatch instead of erroring out, both locally and on Vercel.
legacy-peer-deps=true
